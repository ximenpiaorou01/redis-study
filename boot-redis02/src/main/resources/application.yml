server:
  port: 2222
spring:
  redis:
    database: 0
    host: 192.168.57.130
    port: 6379
    password:
    lettuce:
      pool:
        max-active: 8 #连接池最大连接数(使用负数表示没有现在) 默认8
        max-wait: -1 #连接池最大阻塞等待时间(使用负数表示没有限制) 默认-1
        max-idle: 8 #连接池中最大的空闲连接 默认8
        min-idle: 0 #连接池中最小的连接 默认0
    timeout: 10000
